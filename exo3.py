Amount = int(input("Total amount: "))
numItems = int(input("Number of items: "))
DayOfWeek = str(input("Day of the week: "))

if DayOfWeek == (Monday // Tuesday // Thursday // Friday):
    Amount = Amount * 0.1
elif DayOfWeek == (Saturday // Sunday):
    Amount == Amount * 0.2

if numItems>5:
    Amount = Amount + Amount * 0.05 

